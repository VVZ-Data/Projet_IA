# --- Dépendances d'exécution (Production) ---
# Aucune dépendance externe (utilise la bibliothèque standard Python 3.x)
# Note: tkinter doit être installé sur le système (ex: sudo apt-get install python3-tk)

# --- Outils de développement ---
pylint>=3.0.0
# pytest>=7.0.0  (Optionnel, si vous comptez faire des tests unitaires)
